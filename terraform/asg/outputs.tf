################################################################################
# ASG Module - outputs.tf
#
# This module is consumed internally by the ecs module; the cluster wiring
# (capacity provider + instances) is the useful side-effect and needs no
# outputs. Kept as a placeholder so the file layout matches the other modules.
################################################################################
